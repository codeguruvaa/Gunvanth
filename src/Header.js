import React from 'react';

const Header = () => {
    return (
        <div>

           <header>
                <nav>Home | ABout | Services | Contact</nav>
           </header>
            
        </div>
    );
};

export default Header;